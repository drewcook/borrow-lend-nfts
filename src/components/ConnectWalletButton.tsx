import { MetaMaskButton } from '@metamask/sdk-react-ui'

const ConnectWalletButton = (): JSX.Element => {
	return <MetaMaskButton theme={'dark'} color="blue"></MetaMaskButton>
}

export default ConnectWalletButton
